### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Analityc {
  Clicks: Int
  Contacts: Int
  Views: Int
  id: ID
  reports: Int
  serviseID: Servise
}

type Mutation {
  addServise(category: String!, content: String!, location: Float!, servitorID: String!, title: String!): Servise
  createAnalityc(id: String!): Analityc
  getServiseId(id: String!): Servise
  getUserByID(id: String!): User
  login(email: String!, password: String!): User
  seachServise(category: String, seach: String!): Servise
  signin(contact: Int!, email: String!, name: String!, password: String!): User
  updateAnalityc(Clicks: Int, contact: Int, reports: Int, serviseID: String!): Analityc
}

type Query {
  ok: Boolean!
}

type Servise {
  Reviews: Float
  category: String
  content: String
  id: ID
  location: Int
  servitor: [User]
  title: String
}

type User {
  contact: Int
  email: String
  id: ID
  isValid: Boolean
  name: String
  password: String
  servise: [Servise]
}
